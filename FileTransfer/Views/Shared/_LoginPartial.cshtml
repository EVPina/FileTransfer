@using Microsoft.AspNetCore.Identity
@using FileTransfer.Models.Entities
@inject SignInManager<Usuario> SignInManager
@inject UserManager<Usuario> UserManager

<ul class="navbar-nav">
    @if (User.Identity.IsAuthenticated)
    {
    <li class="nav-item">
        @*<a href="#"><img src="~/images/usuario.svg" id="ImageUser" alt="imagen del usuario" /></a> *@
        @ViewBag.Correo
    </li>
        <li class="nav-item">
            <form id="logoutForm" class="form-inline" method="post" asp-action="CerrarSesion" asp-controller="Account">
                <button id="logout" type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" id="register" asp-action="Register" asp-controller="Account">Register</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" id="login" asp-action="Login" asp-controller="Account">Login</a>
        </li>
    }
</ul>
<script>
   @* const image_user = localStorage.getItem("userimage")
    if (image_user != null) {
        document.getElementById("ImageUser").src = image_user;
    }*@
</script>